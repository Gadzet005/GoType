import { Box } from "@mui/material";

export const TextEditor = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
      }}
    >
      Text editor
    </Box>
  );
};
